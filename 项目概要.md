# Moss Terminal — 项目概要

## 项目定位

Moss Terminal 是一个基于 **kitty** 重构的、深度融合 AI 的终端交互产品。AI 不是外挂插件，而是终端的原生能力——终端里执行过的每一条命令和产生的每一行输出，AI 天然可见。最终交付的是一个**终端应用**（用户运行的就是 Moss Terminal 本身），而不是一个额外安装的 CLI 工具或后台服务。

用户在任意 shell 提示符输入 `》问题`（U+300B 全角书名号前缀）即可直接触发 AI，无需快捷键、无需切换模式；回答以流式彩色文本直接出现在当前终端里。

## 对话模型：交错对话

Moss Terminal 把整个 kitty 窗口的生命周期视为一个对话。对话中每一条用户消息由多组**命令-输出对**加上一个**问题**组成：

```
对话结构：

  User Turn 1:  [A11→B11] + [A12→B12] + ... + C1
    A1n = 第 n 条终端命令        ($ ls, $ gcc main.c...)
    B1n = 该命令对应的终端输出    (total 42, error: undefined...)
    C1  = 用户对 AI 的提问       (》为什么报错？)
    → A 和 B 是交错出现的，每条命令紧跟着它的输出

  Assistant Turn 1:  D1 = AI 回答

  User Turn 2:  [A21→B21] + [A22→B22] + ... + C2
    → 从上次 D1 后的位置开始，读取新的命令-输出对

  Assistant Turn 2:  D2 = AI 回答
```

**与传统聊天的对应关系：**

```
传统聊天： [User msg] → [AI reply] → [User msg] → [AI reply]
                   ↓
Moss Terminal： [A11→B11←A12→B12←...←C1] → [D1] → [A21→B21←...←C2] → [D2]
                 ↑ 一条用户消息由多组命令-输出和问题组成
```

**增量感知的数学定义：**

```
Turn 1:
  line_index 从 0 开始
  收集 A11→B11→A12→B12→A13→B13 → 遇到 C1（》触发）→ 冻结
  发送 [A11→B11→A12→B12→A13→B13→C1] → 得到 D1
  line_index 推进到 D1 之后

Turn 2:
  line_index 从 D1 之后的位置开始
  收集 A21→B21→A22→B22 → 遇到 C2（》触发）→ 冻结
  发送 [A21→B21→A22→B22→C2] → 得到 D2
  line_index 推进到 D2 之后
```

也就是说：系统维护的是**完整**对话历史（含每一轮的 D），但每一轮实际发给模型的终端上下文只是**增量**——上一轮回答之后新出现的命令、输出和这一轮的问题。这个模型现在由引擎内的每窗口 `ContextStore`（`engine/src/ffi/context.rs`）落地：流式回答期间新增的屏幕行会被判定为"引擎自己刚输出的内容"而丢弃，避免回答被喂回下一轮的上下文。

## 三层架构（同一个进程，无 daemon、无 socket）

```
┌─────────────────────────────────────────────────────────────────┐
│                     kitty 进程（单进程，无子进程/无 daemon）       │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Layer 1 · kitty C 核心（10 个补丁 + moss_hook.c/.h 两个新文件）│ │
│  │  screen_linefeed()      → moss_on_line_added()  逐行捕获   │  │
│  │  shell_prompt_marking() → moss_hook_prompt_mark() 命令/退出码│ │
│  │  OSC 7  (cwd 上报)       → moss_hook_cwd()                   │  │
│  │  OSC 7717 (Moss 控制序列) → moss_handle_osc()               │  │
│  │  dlopen($MOSS_ENGINE_LIB 或默认 libmoss.so)；库缺失 = 空操作 │  │
│  │  渲染线程上零系统调用、零日志、零阻塞                        │  │
│  └───────────────────────────────┬───────────────────────────┘  │
│                                   │ 函数指针 / GIL 持有下的回调    │
│  ┌───────────────────────────────▼───────────────────────────┐  │
│  │ Layer 2 · kitty Python 层（moss_integration.py，新文件）     │  │
│  │  ctypes 绑定 libmoss.so；收到 OSC 7717 ask 后调 moss_ask     │  │
│  │  主循环定时器（30ms）排空引擎输出，经 Screen 的 VT parser    │  │
│  │  注入窗口（如同子进程输出）；流结束向 shell 写换行哨兵解除阻塞│  │
│  │  窗口关闭时释放引擎会话                                       │  │
│  └───────────────────────────────┬───────────────────────────┘  │
│                                   │ ctypes FFI（13 个 moss_* 函数）│
│  ┌───────────────────────────────▼───────────────────────────┐  │
│  │ Layer 3 · Moss Engine（Rust，engine/）                       │  │
│  │  同时产出 CLI 二进制 moss 与嵌入式 cdylib libmoss.so          │  │
│  │  （--no-default-features 构建：无 ALSA、无终端 raw mode、    │  │
│  │   无 process::exit 风险；FFI 边界全部 catch_unwind）          │  │
│  │  每窗口独立会话（独立 conversation.db）                       │  │
│  │  命令感知的增量上下文（序列号标记，逐轮只发送新增内容）        │  │
│  │  tokio 双线程运行时；流式事件着色输出队列                     │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

`》` 交互的完整链路：shell integration（zsh 的 `accept-line` widget / fish 的 `\r` 绑定 / bash 的 `command_not_found_handle`，仅当 `MOSS_TERMINAL=1` 时激活）截获 `》` 开头的输入 → 发出 OSC 7717（`ask;<base64 问题>`）→ shell 静默阻塞在一个 `read` 上等待 → 引擎流式回答被注入当前屏幕 → 换行哨兵解除阻塞 → shell 提示符回来。`Ctrl+C` 中断 `read` 会改发 `cancel` OSC，引擎随即停止生成。zsh/bash 均有 300 秒超时保底；bash 这条路径受词法分割限制，zsh/fish 保真度最高。

## 关键决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| Moss Engine 语言 | Rust | 复用已验证的 Miyu 代码库——agent 循环、工具系统、记忆、知识库、缓存分层策略全部平移 |
| 引擎与终端的结合方式 | **FFI 进程内嵌入**（cdylib + C 侧 `dlopen` / Python 侧 `ctypes`），而非独立进程 | 从设计上消灭旧 socket 架构里整整一类缺陷（阻塞 `connect`、串行 `accept`、`window_id` 恒为 0 等）；`》` 到引擎开始处理之间只是一次函数调用，没有进程调度和网络往返 |
| cdylib 安全边界 | `--no-default-features` 剔除 ALSA 音频、终端 raw mode、任何可能 `process::exit` 的路径；FFI 全部 `catch_unwind`，只导出 12 个 `moss_*` 符号 | 引擎现在活在宿主进程（kitty）里，崩溃、异常退出、意外阻塞都会拖垮整个终端，必须让"库缺失 = 空操作""panic = 返回码"成为唯一的失败模式 |
| kitty 源码处理方式 | Fork + 10 个小补丁（约 146 行）+ 4 个 overlay 新文件，而非整仓大改 | 改动面集中、可读，可用 `moss-apply-patches.sh` 在新版 kitty 源码上重放，配合 `moss-sync-kitty.sh` 自动跟随上游更新 |
| AI I/O 方式 | 行内（不分屏），流式着色文本直接写入当前窗口 | 用户确认；不引入新的 UI 范式，符合"终端原生能力"的定位 |
| 触发方式 | `》` 前缀 | 用户确认；U+300B 全角书名号，几乎不会与真实 shell 命令冲突 |
| 每窗口隔离粒度 | 独立的终端上下文环 + 独立 `conversation.db`（`state/windows/<window_id>/`） | 多个 kitty 窗口同时提问时互不干扰、互不污染 |
| Provider 配置 | 独立 `config.jsonc`，`api_key` 支持 `$env:VAR` 间接引用，解析顺序为进程环境 → `~/.config/moss/.env` | 避免明文 key 落盘；与其他项目的配置隔离 |
| 验证方式 | `tests/e2e_mock_llm.py`：`MOSS_HOME` 沙箱重定向全部路径 + 本地起一个 mock LLM，覆盖行捕获、多轮增量、多窗口隔离、取消 | 不依赖真实网络和真实 API key 就能跑通整条链路，可进 CI |
| 代码编辑工具（开发本项目时使用） | Serena MCP 系列（`replace_in_files` / `rename_symbol` 等） | 用户确认 |

## 与旧架构的差异

旧文档描述的是一套 Kitty Kitten（`bridge/`）↔ Unix Socket ↔ 独立 `moss-daemon` 进程的架构。这一整条链路已经**整体废除**：现在三层全部运行在同一个 kitty 进程里，没有 socket、没有 daemon、没有额外的子进程。

- 旧代码里登记在案的约 **80 项缺陷**随整条 socket 链路一起被废除，其中包括：渲染线程阻塞 `connect()`、逐行 `fopen` 写调试日志导致文件描述符泄漏、`accept` 串行阻塞主循环、`window_id` 恒为 0（多窗口互相污染上下文）、上下文行索引存在竞态等。这些缺陷根源在"进程间通信"这个设计本身，换成进程内 FFI 之后，连问题存在的前提条件都不再成立。
- `》` 触发与终端内回显在旧代码里**从未被真正实现过**——旧文档描述的是设计意图，不是已落地的行为。本次是全新实现：三种 shell 各自的 hook（zsh `accept-line` widget / fish `\r` 绑定 / bash `command_not_found_handle`）+ 新增的 OSC 7717 控制序列 + kitty VT parser 注入。
- `bridge/` 目录（`line_stream.py` / `context.py` / `trigger.py` / `io_inline.py`）是旧架构遗留的 Python Kitten，是**死代码**，未被任何补丁、脚本或新架构组件引用，已在本次重构中删除。

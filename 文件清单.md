# Moss Terminal — 文件清单

当前仓库（`moss-terminal-kitty/`）的真实目录结构，以及每个交付物的一句话说明。架构背景见 [项目概要.md](./项目概要.md)，安装与使用见 [部署与使用指南.md](./部署与使用指南.md)。

```
moss-terminal-kitty/
├── README.md                  仓库门面：定位、特性、架构图、快速开始、开发构建、测试
├── 部署与使用指南.md            安装 / 配置 / 启动 / 每日使用 / 故障排查 / 升级
├── 项目概要.md                  定位、对话模型、三层架构、关键决策记录、与旧架构的差异
├── 文件清单.md                  本文件
│
├── engine/                    Moss Engine（Rust）：同一份源码产出 CLI 二进制 moss 与嵌入式 libmoss.so
│   ├── Cargo.toml                package name = "moss"；[lib] crate-type = ["cdylib","rlib"]；[[bin]] moss 需要 feature "cli"
│   ├── LICENSE                   MIT
│   ├── README.md                 引擎自身说明（能力、构建、FFI 一览、MOSS_HOME 沙箱）
│   ├── pics/                     历史遗留插图，文档不再引用，文件本身未删除
│   └── src/
│       ├── ffi/                    mod.rs（13 个 moss_* 导出符号）、context.rs（逐窗口终端上下文环）、output.rs（着色输出队列）、runtime.rs（EngineRuntime / 会话表 / MOSS_HOME 路径解析）
│       ├── agent/                  Agent 主循环；compact.rs、overflow.rs 实现上下文缓存分层裁剪与摘要压缩
│       ├── llm/                    openai_compatible.rs 等 provider 客户端
│       ├── tools/                  run_command / read_file / edit_file / grep / glob / trash_path / web / knowledge_base / memory / mcp / vision / apply_patch 等工具，经 registry.rs 统一注册
│       ├── state/                  conversation_db.rs 等 SQLite 持久化（每窗口一份 conversation.db）
│       ├── memory/                 记忆系统（事实 + 情景联想召回）
│       ├── config.rs / config_tui.rs   配置数据模型 + 交互式配置 TUI
│       ├── paths.rs                标准 XDG 路径解析（MOSS_HOME 覆盖逻辑在 ffi/runtime.rs）
│       ├── cli.rs                  CLI 子命令：ask / init / paths / config / models / history / kb / memory / skills / reset / fish-init / bash-init / zsh-init 等
│       └── ...                     shell/、tools/*、i18n.rs 等其余模块
│
├── kitty/                     已应用 Moss 补丁与 overlay 的 kitty 0.48.1 构建树；可由 pristine 源码 + kitty-patches/ 重新生成
│   ├── kitty/moss_hook.c / moss_hook.h   C 层 hook：dlopen 懒加载 libmoss.so，逐行捕获 / 命令标记 / cwd / OSC 7717 转发；库缺失时全部空操作
│   ├── kitty/moss_integration.py         Python 层：ctypes 绑定 libmoss.so，30ms 定时器排空引擎输出并注入 VT parser，流结束写换行哨兵
│   ├── kitty_tests/moss.py               kitty 自带测试框架下的 moss 模块测试（`./test.py --module moss`）
│   └── LICENSE                           GPLv3（上游 kitty 许可，随构建树带入）
│
├── kitty-patches/             对 kitty 0.48.1 的补丁序列，共 10 个、约 146 行改动
│   ├── 01-screen-c.patch                  screen_linefeed / shell_prompt_marking / OSC 7 里插入 moss_* 调用点
│   ├── 02-vt-parser-c.patch               新增 OSC 7717 分支，分派到 moss_handle_osc
│   ├── 03-setup-py.patch                  构建期读 MOSS_ENABLED（默认 1），=0 时不加 -DMOSS_ENABLED，产出 vanilla kitty
│   ├── 04-window-py.patch                 Window.moss_osc 回调 + Window.destroy 释放引擎会话
│   ├── 05-boss-py.patch                   Boss.start 里触发引擎后台初始化
│   ├── 06-child-py.patch                  子进程环境变量注入 MOSS_TERMINAL=1，作为 shell hook 的启用开关
│   ├── 07-kitty-integration.patch         zsh：接管 accept-line，拦截 》 开头输入
│   ├── 08-kitty-shell-integration-fish.patch  fish：绑定 \r，拦截 》 开头输入
│   ├── 09-kitty-bash.patch                bash：包一层 command_not_found_handle 拦截 》 开头输入
│   └── 10-changelog-rst.patch             docs/changelog.rst 里补一段本 fork 的说明
│
├── kitty-overlay/             随补丁一起整份复制进 kitty 树的 4 个新文件（补丁本身只改已有文件）
│   ├── kitty/moss_hook.c
│   ├── kitty/moss_hook.h
│   ├── kitty/moss_integration.py
│   └── kitty_tests/moss.py
│
├── scripts/
│   ├── moss-apply-patches.sh    <kitty-source-dir> [--check]：把一份 pristine kitty 源码树变成 Moss Terminal 树（打补丁 + 拷贝 overlay）；--check 只校验不改动
│   ├── moss-sync-kitty.sh       检测 Arch/GitHub 上的新版 kitty → 下载并打补丁 → 编译 kitty + engine → 跑 kitty 自身测试 + moss 模块测试；只有 --adopt 才把结果原子换入 kitty/（旧树保留为 kitty.prev/）
│   ├── moss-setup.py            交互式安装脚本（[--dry-run] [--prefix DIR]）：查依赖 → 编译 engine（两次 cargo build）→ 编译 kitty（linux-package）→ 安装到 prefix 并生成 ~/.local/bin/moss-terminal 启动器 → 交互配置 provider/API Key → 写桌面入口 → 汇总
│   ├── moss-start.sh            源码目录直接试跑（开发用）：把 MOSS_ENGINE_LIB 指向 engine/target/release/libmoss.so，exec kitty/kitty/launcher/kitty；不构建、不安装任何东西
│   └── moss-terminal.desktop    桌面入口模板；moss-setup.py 安装时把其中的 Exec/TryExec 替换成实际生成的启动器绝对路径
│
├── packaging/                 可选、默认不安装、只交付文件本身的打包资源
│   ├── moss-kitty-sync.hook             pacman hook：系统 kitty 包升级后仅 touch 一个 flag 文件，不以 root 身份跑任何用户代码
│   ├── moss-sync-check.service / .timer  systemd user 定时器，每天跑一次 `moss-sync-kitty.sh --notify-only`（只检测/通知，不下载不编译不替换）
│   └── README.md                        以上文件的手动安装 / 卸载说明
│
├── specs/需求规格.md            需求规格 v2：核心对话模型、User Stories、功能需求、边界条件、成功指标、不做的事
└── tests/                      沙箱化端到端测试（全部 MOSS_HOME 重定向 + 本地 mock OpenAI 兼容 SSE 服务器，零外发请求）
    ├── e2e_mock_llm.py             上下文组装、多轮增量、多窗口隔离、busy/取消、窗口关闭清理
    ├── e2e_kitty_capture.py        经真实 kitty C hook 的捕获链路（eza 图标/CJK/OSC133/触发行去重），需 kitty +launch
    ├── e2e_run_command_embedded.py 嵌入宿主收割子进程时 run_command 不得假失败（ECHILD 回归），需 kitty +launch
    └── e2e_answer_leak.py          AI 回答不得泄漏进下一轮增量（注入回环时序回归）
│
```

仓库根目录下还有一批历史执行记录 / 提示词文档（如「实施指南.md」「全面测试异常清单.md」等），它们是项目推进过程中的过程性材料，不是本项目对外交付的一部分，此处不逐一列出。

## 构建产物说明：为什么 `libmoss.so` 要编译两次

`engine/Cargo.toml` 里 `moss` 二进制目标声明了 `required-features = ["cli"]`，而 `cli` feature 又依赖 `rodio`（音频）等一整套"独占终端"的机制（REPL、配置 TUI、raw-mode 终端接管、闹钟音频、必要时的 `process::exit`）。这些机制放进独立运行的 CLI 进程里是安全的，但如果原样链接进 `libmoss.so` 再被 `dlopen` 进 kitty 自己的进程地址空间，任何一处 raw-mode 接管或音频初始化失败都可能拖垮整个终端。

因此正式构建固定分两步：

```bash
cargo build --release                       # 1) 默认 features（含 cli），产出完整的 moss CLI 二进制
cargo build --release --no-default-features # 2) 剔除 cli 相关机制，重新链接出内嵌安全版 target/release/libmoss.so
```

第二步不会重新触碰 `moss` 二进制（它需要 `cli` feature，`--no-default-features` 下会被跳过），只会覆盖 `libmoss.so`。顺序不能颠倒——先跑第二步再跑第一步，`target/release/libmoss.so` 会被第一次编译的产物覆盖回带音频栈的版本。`scripts/moss-setup.py` 的 `build_engine()` 就是按这个顺序调用的。

# Moss Terminal — 部署与使用指南

架构背景见 [项目概要.md](./项目概要.md)；目录结构逐条说明见 [文件清单.md](./文件清单.md)。本指南只讲怎么装、怎么配、怎么用、出问题怎么查、怎么升级。

Moss Terminal 没有任何 daemon 需要单独启动或保活——AI 引擎是一个被 kitty 进程自己 `dlopen`（C 层）/ `ctypes`（Python 层）加载进来的动态库（`libmoss.so`），随 kitty 进程生死。

## 1. 安装

### 1.0 准备 kitty 构建树（克隆仓库后首次必做）

仓库**不包含** `kitty/` 目录——那是一棵 220 MB 的生成物，已被 `.gitignore` 排除。真正的源头是 `kitty-patches/`（10 个补丁）+ `kitty-overlay/`（4 个新文件），任何时候都能从官方 kitty 源码重新生成出完全一样的构建树。下面全部命令在仓库根目录执行：

```bash
# 1) 下载锁定版本的官方 kitty 源码，并校验 sha256（务必校验）
curl -LO https://github.com/kovidgoyal/kitty/releases/download/v0.48.1/kitty-0.48.1.tar.xz
echo 'aadb428e20ad678c0a7969c0a80c46f391b49addeb7fda57b06a14a4d102fb1d  kitty-0.48.1.tar.xz' | sha256sum -c -
tar xf kitty-0.48.1.tar.xz

# 2) 应用 Moss 补丁序列 + 覆盖文件
./scripts/moss-apply-patches.sh kitty-0.48.1 --check   # 先干跑：补丁是否还能干净应用
./scripts/moss-apply-patches.sh kitty-0.48.1           # 真正应用

# 3) 采纳为构建树
mv kitty-0.48.1 kitty
```

做完这一步，下面 1.1 / 1.2 / 1.3 里所有涉及 `kitty/` 的命令才有对象。`scripts/moss-sync-kitty.sh` 也能替你完成同样的事（见第 6 节），区别是它面向"跟随上游升级"场景，会额外做编译与测试。

**不要直接修改 `kitty/` 里的文件**——它随时会被重新生成而丢失。改动一律以补丁形式落在 `kitty-patches/`，流程见 [CONTRIBUTING.md](./CONTRIBUTING.md)。

### 1.1 一键安装（推荐）

> 前置：先完成 §1.0，仓库里必须已经有 `kitty/` 构建树，否则第 3 阶段无从谈起。

```bash
python3 scripts/moss-setup.py            # 正式安装
python3 scripts/moss-setup.py --dry-run  # 先预览：不构建、不写任何文件、不读任何交互输入
python3 scripts/moss-setup.py --prefix ~/apps/moss-terminal  # 自定义安装前缀（默认 ~/.local/share/moss-terminal）
```

脚本按顺序跑七个阶段：

1. **检测依赖**——硬依赖 `cargo`、`rustc`、`python3 >= 3.8`、`gcc`/`clang`（缺一个会终止安装，`--dry-run` 下只警告）；`pkg-config`、`go`（编译内置 kittens 用）以及 kitty 构建常用的 `harfbuzz`/`fontconfig`/`freetype2`/`cairo`/`openssl`/`wayland-protocols` 等 pkg-config 包是软依赖，缺失只警告不阻断。注意脚本**不**检测 `simde`（Arch 包名 `simde`，Debian/Ubuntu 为 `libsimde-dev`），但它是编译 kitty 的硬依赖——`kitty/simd-string-impl.h` 在 x86-64/ARM64 上无条件 `#include <simde/x86/avx2.h>` 且 kitty 未内置它；缺失时第 3 阶段会以一句找不到头文件的编译错误失败。Arch 上一次性装齐：`sudo pacman -S --needed rust go python cairo dbus freetype2 fontconfig harfbuzz lcms2 libgl libpng librsync libx11 libxcursor libxkbcommon libxkbcommon-x11 libxi openssl simde wayland wayland-protocols xxhash zlib alsa-lib base-devel`。
2. **编译 Moss Engine**——依次跑两次 `cargo build --release`：先默认 features 编出完整的 `moss` CLI 二进制，再加 `--no-default-features` 重新链接出内嵌安全版 `libmoss.so`（去掉了音频/raw-mode 等假设独占终端的机制）。两个产物都在 `engine/target/release/` 下。
3. **编译 kitty**——在 `kitty/` 目录下跑 `python3 setup.py linux-package`，产出 `kitty/linux-package/`。
4. **安装**——把 `kitty/linux-package/` 整份复制到安装前缀（默认 `~/.local/share/moss-terminal`）；`libmoss.so` 被放到 `<prefix>/lib/kitty/libmoss.so`——这正是 `moss_integration.py` 自动搜索引擎库的位置，零环境变量也能被发现；`moss` CLI 复制到 `~/.local/bin/moss`；再生成一个启动器脚本 `~/.local/bin/moss-terminal`（内容是 `export MOSS_ENGINE_LIB=<prefix>/lib/kitty/libmoss.so` 后 `exec` 安装好的 kitty 二进制）。若 `~/.local/bin` 不在 `PATH` 里会提示你加进去。
5. **配置 AI Provider**——交互式：四个预设可选（`opencode Zen` 公共端点，免 API Key / `DeepSeek` / `OpenAI` / 自定义 OpenAI 兼容 API），依次问模型列表与默认模型；若需要 Key，用 `getpass` 隐藏输入，写入 `~/.config/moss/.env`（权限 `0600`，变量名自动生成为 `MOSS_<PROVIDER_ID大写>_API_KEY`）。`config.jsonc` 是**增量合并**写入：先把旧文件备份成 `config.jsonc.bak.<时间戳>`，只更新这一个 provider 条目和 `active_provider`，其余键原样保留——重复运行安装向导添加第二个 provider 不会把第一个冲掉。
6. **创建桌面入口**——读取 `scripts/moss-terminal.desktop` 模板，把其中 `Exec=`/`TryExec=` 换成第 4 步生成的启动器绝对路径，写到 `~/.local/share/applications/moss-terminal.desktop`。
7. **完成小结**——打印安装前缀、kitty 二进制、`moss` CLI、`libmoss.so`、启动器、桌面入口的具体路径，以及最基本的用法提示。

### 1.2 从源码目录直接试跑（开发用，不安装到系统任何地方）

> 前置：先完成 §1.0（`kitty/` 是生成物，克隆下来的仓库里没有）。

```bash
cd engine
cargo build --release
cargo build --release --no-default-features
cd ../kitty
make debug
cd ..
./scripts/moss-start.sh
```

`moss-start.sh` 不构建、不安装任何东西：它只是把 `MOSS_ENGINE_LIB` 指向 `engine/target/release/libmoss.so`，然后 `exec` 源码树里 `make debug` 产出的 launcher（`kitty/kitty/launcher/kitty`）。适合边改代码边验证，不用每次都跑一遍完整的 `moss-setup.py`。

### 1.3 手动安装（不借助脚本）

等价于 `moss-setup.py` 内部做的事：先按 §1.0 生成 `kitty/` 构建树 → 两次 `cargo build`（同上）→ `cd kitty && python3 setup.py linux-package` → 把 `kitty/linux-package/` 拷到你想要的位置 → 把 `libmoss.so` 放进 `<那个位置>/lib/kitty/libmoss.so` → 手动创建 `~/.config/moss/config.jsonc` 和 `~/.config/moss/.env`（见下一节模板）。想要精确对照每一步命令，直接读 `scripts/moss-setup.py` 是最权威的参考。

## 2. 配置

### 2.1 config.jsonc

路径：`~/.config/moss/config.jsonc`（遵循 `$XDG_CONFIG_HOME`；若设置了 `MOSS_HOME`，则是 `$MOSS_HOME/config/config.jsonc`，用于沙箱/测试）。

```jsonc
{
  "active_provider": "deepseek",
  "providers": [
    {
      "id": "opencode",
      "display_name": "opencode Zen",
      "base_url": "https://opencode.ai/zen/v1",
      "models": ["big-pickle"],
      "default_model": "big-pickle"
      // 公共免费端点，不需要 api_key
    },
    {
      "id": "deepseek",
      "display_name": "DeepSeek",
      "base_url": "https://api.deepseek.com",
      "api_key": "$env:MOSS_DEEPSEEK_API_KEY",
      "models": ["deepseek-chat", "deepseek-reasoner"],
      "default_model": "deepseek-chat"
    },
    {
      "id": "openai",
      "display_name": "OpenAI",
      "base_url": "https://api.openai.com/v1",
      "api_key": "$env:MOSS_OPENAI_API_KEY",
      "models": ["gpt-4o-mini"],
      "default_model": "gpt-4o-mini"
    }
  ],
  "prompt": {
    "active_persona": ""
  }
}
```

`active_provider` 同一时间只生效一个；其余 provider 条目留着即可，随时改 `active_provider` 切换。也可以用 `moss-setup.py` 里的"自定义"预设接入任意 OpenAI 兼容 API。

### 2.2 API Key 与 .env

`api_key` 字段支持 `$env:VAR_NAME` 间接引用，解析顺序是：**进程环境变量优先，其次 `~/.config/moss/.env`**（或 `$MOSS_HOME/config/.env`）。`.env` 是简单的 `KEY="value"` 逐行格式，权限务必是 `0600`：

```bash
mkdir -p ~/.config/moss
printf 'MOSS_DEEPSEEK_API_KEY="sk-你的key"\n' >> ~/.config/moss/.env
chmod 600 ~/.config/moss/.env
```

`moss-setup.py` 走交互流程时会自动完成这一步（`getpass` 隐藏输入，写文件后 `chmod 600`），不需要手动编辑。

### 2.3 Persona（角色设定）

在 `config.jsonc` 的 `prompt.active_persona` 里填一个人格名字（对应 `~/.config/moss/prompts/<名字>` 这个文件，留空则用引擎内置的默认人格）。改完配置后**不需要重启 kitty**——在任意一个 Moss Terminal 窗口里执行：

```bash
printf '\033]7717;reload-config\007'
```

即可让引擎重新读取 `config.jsonc`；空闲会话的下一次提问会用上新配置（正在流式输出中的请求不受影响，会用旧配置跑完）。

## 3. 启动

- 已用 `moss-setup.py` 安装：运行 `moss-terminal`，或从应用菜单里点 "Moss Terminal"。
- 从源码目录直接跑（开发）：`./scripts/moss-start.sh`。

两种方式都不需要额外启动或守护任何后台进程。

## 4. 每日使用

正常敲命令，AI 已经在后台看着这个窗口里发生的一切。遇到问题时：

```
$ gcc main.c
main.c: In function ‘main’:
main.c:3:5: error: expected ‘;’ before ‘return’

》为什么报错
```

按下回车后，AI 的回答会流式出现在当前行下方：思考过程是绿色文字，工具调用是蓝色，正文是默认色。回答结束后 shell 提示符自动回来，不需要任何额外操作。

- **打断**：回答过程中按 `Ctrl+C`，会中断 shell 侧的等待并向引擎发送取消，引擎立刻停止生成。
- **多窗口 = 多会话**：每个 kitty 窗口都有自己独立的终端上下文和 `conversation.db`，在三个窗口里同时问三件不相关的事，互不干扰。
- **增量上下文**：第二次 `》` 提问时，AI 看到的是"上一次回答之后新发生的命令和输出 + 这次的问题"，但它仍然记得更早的对话历史（完整历史由引擎维护，见 [项目概要.md](./项目概要.md) 的对话模型一节）。

## 5. 故障排查

| 现象 | 排查方向 |
|------|------|
| `》` 提问后立刻出现 "moss engine unavailable" / 类似的红色错误行 | `libmoss.so` 没找到。检查 `MOSS_ENGINE_LIB` 环境变量是否指向真实存在的文件；用 `moss-start.sh` 时确认已经跑过 `cargo build --release --no-default-features`；正式安装后应该在 `<prefix>/lib/kitty/libmoss.so` |
| 输入 `》问题` 后完全没反应，`》` 被当成普通命令交给了 shell | 确认当前这份 kitty 不是 `MOSS_ENABLED=0` 编译出来的 vanilla 版（那种构建下所有 hook 都是空操作）；确认当前 shell 会话里 `echo $MOSS_TERMINAL` 是 `1`——这个变量只有由 Moss Terminal 自己派生的子进程才会有，直接在普通终端里 `source` shell 配置不会生效 |
| 提问后长时间没有任何输出 | 看 `~/.cache/moss/logs/`（或 `$MOSS_HOME/cache/logs/`）；用 `MOSS_LOG=debug` 重新触发一次问题，日志级别会提到 debug；同时检查网络连通性和 `config.jsonc` 里对应 provider 的 `base_url`/`api_key` 是否正确 |
| `MOSS_ENABLED=0` 编译的 kitty 不会触发 `》` | 这是预期行为：该构建下 C 层所有 moss hook 都编译成空函数，属于"纯净版" kitty，用于对照测试或不想要 AI 功能的场景 |
| bash 下问题里的引号、通配符、多余空格被吃掉 | bash 是通过 `command_not_found_handle` 拦截的，`$*` 已经过一次 shell 词法分割；需要原样保真传递问题文本，请用 zsh 或 fish |
| 提问 300 秒后自动被打断，回到提示符 | zsh/bash 侧 `read -t 300` 的超时保底：300 秒内引擎既没结束流式输出、你也没按 `Ctrl+C`，shell 就会自动发送取消并把提示符还给你，避免网络异常导致永久卡死。fish 的 `read` 无超时参数，该 shell 下依赖 `Ctrl+C` 手动取消 |
| 想切回原版 kitty | 单独用 `MOSS_ENABLED=0` 编译一份 kitty，或者直接运行系统自带的 `kitty`；它和 `moss-terminal` 是两个独立的启动器，互不影响 |

## 6. 升级（跟随上游 kitty）

```bash
./scripts/moss-sync-kitty.sh --notify-only              # 只检测新版本并提示，不做任何改动
./scripts/moss-sync-kitty.sh --check-only                # 补丁能否在当前目标版本上干净应用的预演，不编译
./scripts/moss-sync-kitty.sh                              # 完整跑一遍：下载 → 打补丁 → 编译 kitty + engine → 跑 kitty 自身测试 + moss 模块测试
./scripts/moss-sync-kitty.sh --version 0.49.0             # 指定目标版本而不是自动探测
./scripts/moss-sync-kitty.sh --adopt                      # 在上一条通过之后，把结果原子换入 kitty/，旧树保留为 kitty.prev/
./scripts/moss-sync-kitty.sh --skip-engine                # 只有 kitty 本身变化、engine 不需要重编时可以跳过
```

不带 `--adopt` 时，整个过程都在 `.sync-work/` 下的临时目录里进行，仓库和现有的 `kitty/` 完全不受影响，可以随便跑来看结果。只有跑通全部测试并显式加上 `--adopt`，才会把新树原子换进 `kitty/`（用 `mv`，旧树整份保留在 `kitty.prev/`，随时可以手动换回去）。补丁冲突时脚本会报出具体哪个 `.patch` 文件冲突，需要针对上游改动手动 rebase。

可选、默认不安装的自动化在 `packaging/` 目录：一个 pacman hook（系统 kitty 包升级后只打一个 flag 文件，不以 root 跑任何用户代码）和一对 systemd user 定时器（每天跑一次 `moss-sync-kitty.sh --notify-only`）。两者都需要照着 `packaging/README.md` 手动安装，`moss-setup.py` 不会自动帮你装。
